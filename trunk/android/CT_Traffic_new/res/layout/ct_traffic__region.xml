<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <LinearLayout
        android:id="@+id/load_progress_layout"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_centerInParent="true"
        android:gravity="center" >

        <ProgressBar
            android:id="@+id/progressbar"
            style="@style/CT__AppTheme.ProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center" />

        <TextView
            android:id="@+id/no_data"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textSize="18sp"
            android:visibility="gone" />
    </LinearLayout>

    <ListView
        android:id="@+id/region_lv_regions"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/ct_traffic__color_list_backgroud"
        android:cacheColorHint="@color/ct_traffic__color_list_cache_hint"
        android:divider="@drawable/ct_traffic__divider"
        android:fastScrollEnabled="true"
        android:listSelector="@drawable/ct_traffic__list_selector"
        android:visibility="gone" >
    </ListView>

</RelativeLayout>